# Local Development

The project metadata is stored in `projects.yaml`.
YAML is easier to update, but it needs to be converted to JSON to be used in Javascript.

Use `npm run convert` to update the project data.

Run local server with `python3 -m http.server 3000`
